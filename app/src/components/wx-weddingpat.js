var React = require('react');
var WXScheme = require('./wx-scheme.js');

var WXWeddingPat = React.createClass({

    render: function() {
        return (
            <WXScheme />
        );
    }

});

module.exports = WXWeddingPat;
